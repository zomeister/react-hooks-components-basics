import React from "react";

/**
 * Represents a comment
 * @returns JSX element 
 */
function Comment() {
  // build components with JSX, don't forget to
  return <div>Naturally, I agree with this article.</div>;
}

/**@export Comment: JSX Component */
export default Comment;
